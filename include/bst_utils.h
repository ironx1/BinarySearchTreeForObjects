#if !defined(BST_UTILS_H)
#define BST_UTILS_H

#define memAllocM(type, size) (type*)malloc(sizeof(type)*(size))

typedef void** voidptr_t;
typedef char* charptr_t;
typedef float* folatptr_t;
typedef int* pint_t;
#endif // BST_UTILS_H
